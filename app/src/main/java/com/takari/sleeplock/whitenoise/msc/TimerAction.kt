package com.takari.sleeplock.whitenoise.msc

import com.takari.sleeplock.R

enum class TimerAction(val iconResource: Int) {
    Play(R.drawable.transparant_play_icon),
    Pause(R.drawable.transparant_pause_icon),
    Reset(R.drawable.reset)
}